version: "3.8"
services:
  api:
    build: .
    image: todo-api:latest
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
    volumes:
      - ./data:/usr/src/app/data
    restart: unless-stopped
